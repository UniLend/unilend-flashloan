export interface Wallet {
  id: number;
  name: string;
  icon: string;
}
export type HeadingProps = {
  className?: string,
  children?: React.ReactNode
}

export type DividerProps = {
  className?: string
}
